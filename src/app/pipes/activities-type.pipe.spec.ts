import { ActivitiesTypePipe } from './activities-type.pipe';

describe('ActivitiesTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivitiesTypePipe();
    expect(pipe).toBeTruthy();
  });
});
